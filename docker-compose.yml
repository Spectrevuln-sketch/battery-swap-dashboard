services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: ecgo
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - ecgo_pgdata:/var/lib/postgresql/data
volumes:
  ecgo_pgdata:
